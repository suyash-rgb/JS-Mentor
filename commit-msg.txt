refactor: Why Choose JS Mentor section

I switched the features-grid to use a responsive flexbox layout instead of a rigid CSS grid. Now, when the screen is wide enough to fit 4 cards in the top row, the remaining 2 cards will gracefully wrap to the next row and be perfectly centered in the middle of the screen, rather than being stuck on the left.