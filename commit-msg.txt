refactor: domain specialized ai-assistant

created hook useDomainSpecializedAiAssistant and service function domainSpecicalizedAssistantService in src/utils/groqService.js

Redundancy Eliminated: All the axios calls and isJavaScriptRelated logic are gone, replaced by the askAi function from your hook.

Shared State: Both Chatbot.js and Ai.js now share the exact same error and isLoading behavior, meaning if you change the rate limit on the backend, both components will handle it identically.

State Management: Added responseType to distinguish between an AI answer and a success message from the Doubt Scheduler. This prevents the "Read More" button from appearing on a simple "Doubt registered" notification.

Clean Navigation: The handleReadMore function correctly passes the originalQuestion so the text area on the AI page is pre-filled, making the transition feel seamless for the student.

Changes made
Created compilerAiService.js

Posts to http://127.0.0.1:8000/ai/js-mentor/explain-error
Sends { code, error_message: consoleOutput }
Created useCompilerAi.js

Manages isLoading, error, and explanation
Handles 429 by extracting err.response.data.detail and exposing it through explanation
Uses service layer for the request
Updated compiler.js

Switched from explainErrorWithAI to useCompilerAi
Uses current code and consoleOutput from useCompilerCore
Passes hook-managed loading and explanation to AiMentorModal
Updated jscompiler.js

Same hook integration as compiler.js
Ensures both compiler page variants use the new backend service
Cleaned up compilerUtils.js

Removed hardcoded GROK API URL / key logic
Left only transpilation and mock wrapper utilities
Notes
AiMentorModal remains compatible because it still receives a Markdown string via explanation
No frontend GROK secrets remain in compilerUtils.js