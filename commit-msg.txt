feat: fully automated the doubt session scheduling engine

Automated Scheduling: Integrated APScheduler into the FastAPI lifespan, configured to run the scheduling engine every day at 10:00 AM.

Removed Manual Trigger: Deleted the POST /api/v1/schedule/run endpoint and its associated schemas, ensuring trainers no longer need to manually trigger the engine.

Race Condition Protection: Implemented FOR UPDATE SKIP LOCKED in the scheduling logic to ensure safety during concurrent runs or multi-instance deployments.

Standalone Script: Created scripts/run_automation.py as a fail-safe or external trigger option.

Cleanup: Pruned unused imports and schemas from scheduling.py to keep the codebase clean.

The backend is now fully automated and "hands-off" for the trainers!

- closes #11 
