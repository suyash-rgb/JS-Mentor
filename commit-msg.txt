feat: add live dialogue transcription and summary

Live Dialogue Transcription & Real-Time Caching:
- Added in-memory session transcript buffer in app/routers/signaling.py to record spoken dialogue fragments with speaker identification and timestamps.
- Attached completion handler to session end and disconnect events to automatically trigger post-class summarization in the background.

AI Chunked Summarization Engine (0-Cost GPT OSS 20B):
- New service app/services/summary_service.py implementing text-only Map-Reduce summarization designed for low latency and zero API cost.
- Added chunk_transcript utility to safely partition long lecture transcripts and avoid exceeding the 8,000-token context limit.
- Added generate_summary, summarize_chunk, and merge_summaries to synthesize comprehensive study notes from classroom discussions.
- Emits real-time 'class_summary_ready' Socket.IO notification to all connected students upon generation completion.

Database Models & Schemas:
- Modified app/models/learning.py to introduce ClassSummary (with 48-hour expiration TTL for temporary caching) and StudentNote (for persistent personal student study notes).
- Added corresponding Pydantic schemas in app/schemas/curriculum_note.py (ClassSummaryResponse, StudentNoteResponse, StudentNoteUpsert).

Student Notes & Summary Import REST API:
- Added GET /api/v1/curriculum/notes/{path_id} to retrieve a student's customized notes with automatic fallback to trainer course notes.
- Added PUT /api/v1/curriculum/student/notes/{path_id} to create and update personal student notes.
- Added POST /api/v1/curriculum/notes/{path_id}/import-summary/{summary_id} to merge temporary class summaries into personal study notes.

Automated Verification Suites:
- Added app/tests/test_summary_service.py unit tests verifying dialogue chunking and word-budget constraints.
- Added app/tests/test_notes_api.py unit tests verifying student notes CRUD and summary import integration.

References #37
