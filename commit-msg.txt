feat: signaling & notification of doubt sessions

app/routers/signaling.py (MODIFIED)

Global User Rooms: We updated the main Socket.IO connect event. Now, whenever a user connects to the signaling server, the backend automatically subscribes them to a private room named global_user_{user_id}.
Video Call Alerts: Inside the initiate_call function, right after it rings the session room, we added logic to emit the global-incoming-session event directly to the student's global_user_{student_id} room.

app/routers/chat.py (MODIFIED)

Cross-Protocol Alerts: Your text chat uses standard FastAPI WebSockets, while the video call uses python-socketio. We bridged them!
Now, inside the chat loop, right after a chat message is successfully saved to the database, we check if the sender is a TRAINER.
If it is a trainer, we import the sio instance from the signaling router and use it to fire the global-incoming-session event to the student's global room. This ensures that a simple "Hello" text from the trainer is enough to force the student's chatbot to slide open!