feat: implement AI-driven error explanation

Core AI Features:

Contextual Error Analysis: Integrated Mutation Observer logic and Groq API wrapper to provide real-time, beginner-friendly explanations of JavaScript runtime errors to users.

Structured Mentor Prompting: Optimized AI prompts to deliver short, table-free responses focusing on analogies, line-specific fixes, and "Pro-Tips".

Intelligent Response Parsing: Implemented a robust extraction logic to target output_text within the Groq /responses nested JSON structure while bypassing internal reasoning data.

Markdown Presentation: Integrated ReactMarkdown and remark-gfm within the UI to render technical explanations with proper code syntax highlighting and formatting.

Compiler & UI Enhancements:

DOM Protection: Refactored the execution engine to mock document.write, preventing it from clearing the application state during code runs.

ResizeObserver Stabilization: Added a global error guard to suppress ResizeObserver loop notifications, ensuring a stable development overlay during layout shifts.

Accessibility & Focus Management: Resolved aria-hidden and focus-trapping issues by configuring Material UI Modal with disableRestoreFocus and disableEnforceFocus.

Debounced Execution: Implemented a 600ms debounce on the compiler engine to optimize performance and prevent excessive API calls during active typing.

Architecture & Security:

Axios Migration: Transitioned from fetch to axios for standardized request handling and improved error logging.

Environment Variable Integration: Secured sensitive API configurations (URL, Key, Model) using process.env with fallback defaults for deployment resiliency.

