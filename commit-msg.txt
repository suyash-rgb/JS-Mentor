feat: Integration for Challanges Section

Integration (trainerService.js)

Added wrapper functions for addExercise, updateExercise, and deleteExercise.
Connected them to the existing POST /add-exercise, PUT /exercises/{ex_id}, and DELETE /learning-paths/delete-exercises/{ex_id} backend endpoints.

Frontend UI (CurriculumManager.js)

No React Flow: Since challenges are essentially elaborate coding prompts (Title, Description, Difficulty, Tags), I built a clean, standard Material-UI form Dialog.
Path & Topic Selection: When creating a new challenge, the form uses the exact same getTopicsForLearningPath API logic we built for quizzes to let the trainer select exactly which page the challenge belongs to.
Edit Mode: Clicking "Edit" on a challenge card pre-fills the form with the existing data.
Validation & Deletion: Added safety checks before deletion and toast notifications for successful CRUD operations.
You can now freely Create, Update, and Delete challenges in the Curriculum Manager, and they will sync flawlessly with the backend data.json structure!