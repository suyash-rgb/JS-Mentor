feat: auth refactor and risk prediction model

- Integrated a multinomial logistic regression model (`risk_model.joblib`) for predicting student dropout risk (`/predict_risk`) based on progress and quiz metrics.
- Added ML training pipeline (`app/ml/train.py`) and data generation scripts.
- Refactored Authentication to strictly enforce Trainer-only login via the `/login` endpoint.
- Removed legacy `scholar_no` authentication and dependency across models, schemas, and services.
- Added `/register/trainer` API to support Institute Trainer onboarding.
- Aligned `UserRole` ENUM with DB schema (STUDENT, TRAINER).
- Added new SQLAlchemy models (`StudentProgress`, `ExerciseEvaluation`, `QuizEvaluation`, `StudentRiskPrediction`) to represent the new DB schema for ML data.
- Fixed rigid `NoneType` bugs in `authenticate_user` and added support for login via both `username` and `email`.
