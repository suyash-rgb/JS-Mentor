feat: add mermaid chart support in curriculum notes

Installed library: Added mermaid package to dynamically render diagrams from code blocks.
Custom Component: Created a new Mermaid.js component to handle the async rendering of charts and catch syntax errors cleanly.
Configured Theme: Set the default mermaid theme to "neutral" to match the platform's aesthetics.

Updated Editor:
Added a new Mermaid chart icon (AccountTreeIcon) to the NotesEditorPage toolbar.
Clicking the icon inserts a ready-to-use graph template so trainers don't have to remember the exact syntax.
Intercepted the markdown code renderer to route "language-mermaid" blocks through the new Mermaid component for live previews.

Updated Viewer:
Modified NotesViewerPage to also intercept "language-mermaid" blocks, ensuring students see the fully rendered SVGs instead of raw markdown text.
