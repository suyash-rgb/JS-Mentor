feat: Add Curriculum Notes API

- Implemented `CurriculumNote` database model and schemas
- Added `get_curriculum_note` and `upsert_curriculum_note` to curriculum service
- Added GET `/curriculum/notes/{path_id}` endpoint to fetch notes
- Added PUT `/curriculum/notes/{path_id}` endpoint to update or create notes
- Cleaned up miscellaneous comments in `generate_local_data.py`
