fix: Include exercise question in grading submissions

Updated the `SubmissionDetail` schema to include `exercise_question`.
Refactored `get_grading_submissions` in `app/routers/trainer.py` to parse `data.json` and dynamically map the raw `exercise_id` to its actual human-readable `title` and `description` (question).
This ensures that when a trainer opens the grading modal, they can see exactly what question the student was trying to answer alongside their code submission.
