refactor: externalize CSS and implement responsive markdown table wrapper

Description:

- Decoupled Styles from Logic: Extracted massive inline style objects from the Ai.js component into a dedicated Ai.css stylesheet to improve maintainability and readability.

- Fixed Mobile Table Responsiveness: Implemented a table-responsive-wrapper within the ReactMarkdown components configuration. This adds a scrollable container with overflow-x: auto around generated tables to prevent layout breaking on small screens.

- Optimized Component Architecture: Simplified the ReactMarkdown implementation by utilizing CSS class selectors for headers, code blocks, and list items instead of repetitive inline JSX styling.

- Improved UX: Added min-width constraints to table elements to ensure text remains legible during horizontal scrolling and added WebkitOverflowScrolling: touch for smooth scrolling on iOS devices.

- Cleaned Up JSX: Refactored the main render block to use semantic class names (ai-main-card, ai-submit-btn, etc.), reducing the component's line count significantly.