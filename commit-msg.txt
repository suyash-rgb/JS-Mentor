refactor: filtering ML driven risk assessment

I have refactored the ML service to specifically target students who have completed the first two learning paths ("Fundamentals" and "JavaScript Core").

Backend Filtering (app/services/ml_service.py):
 - Implemented a new logic to automatically extract all topic IDs from the first two cards of data.json.
 - Added a specialized query to StudentProgress that identifies students who have achieved COMPLETED status for every required topic in those paths.
 - Updated the get_high_risk_students endpoint to only run predictions for this qualified cohort, ensuring the batch analysis is focused on the correct stage of the student journey.

Verification: I created and successfully executed a test suite scratch/test_ml_filter.py which confirmed that:
 - Students who finished all topics are correctly qualified for prediction.
 - Students with partial progress are excluded from the risk assessment.