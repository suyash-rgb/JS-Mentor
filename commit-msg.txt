fix: synchronized renderer with standardized JSON schema

- Overhauled 'renderDynamicSections' to use index-aligned lookup (titleN -> paraN -> codeN -> resultN).
- Restored missing code snippets in Fundamentals and JS Core caused by previous sequential key assumptions.
- Prioritized 'paraN' as the primary description key to match the new strict uniform structure.
- Future-proofed the Frontend path with a hybrid lookup that handles both index-aligned and sequential data.
- Verified CSS consistency across all paths to ensure code blocks render correctly.