feat: video upload enhancemnts

API Service: Updated src/utils/trainerService.js to match the new /api/v1/curriculum/ path for all curriculum-related calls (quizzes, exercises, and videos).
Media Manager: Modified handleAddVideo in MediaManager.js to consistently use FormData for submissions. This ensures that both local file uploads and external URL additions are compatible with the enhanced backend logic.

updated the MediaManager gallery to detect whether a video is from YouTube or Cloudinary.
For Cloudinary videos: The app now uses a Native HTML5 Video Player (<video> tag) with built-in controls and autoplay. This ensures high-performance streaming directly from Cloudinary's global delivery network.

Automatic Thumbnails for Local Videos

Cloudinary Intelligence: I've implemented a logic that automatically generates a preview thumbnail for any video uploaded to Cloudinary.
It selects a representative frame from the video and applies a transformation (so_auto,c_scale,w_500) to ensure the gallery looks consistent, even for non-YouTube content.
The dashboard is now fully optimized for both external embeds and local high-quality video hosting!

