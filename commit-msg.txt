feat: implement video progress tracking

Models & Schemas: Introduced the VideoProgress database model in learning.py and the corresponding VideoProgressUpdate Pydantic schema in analytics.py.
API Endpoints: Added POST /api/v1/student/video to log video watch progress and GET /api/v1/student/topic-status/{topic_id} to retrieve completion statuses of individual videos, quizzes, and exercises for a topic in student.py.
Dynamic Completion Engine: Implemented evaluate_topic_completion() in student_service.py. The backend now strictly validates that a student has completed all required videos, quizzes, and exercises dynamically mapped from data.json before allowing a topic to be marked as COMPLETED.
Integration: Updated log_exercise and log_quiz to invoke the new completion evaluation automatically upon successful submissions.