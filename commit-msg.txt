fix: prevent print dialog in JS compiler

- Shadow 'print' as undefined in execution context to prevent browser dialog.
- Ensure 'print()' call triggers standard ReferenceError/TypeError for pure environment.
- Restore native support for 'alert', 'confirm', and 'prompt' as requested.
- Use new Function() safely in both compiler.js and jscompiler.js.
- Add fallback 'window.print' override during code execution for browser safety.