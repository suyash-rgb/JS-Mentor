feat: add composite unique constraint to student notes for postgres concurrency

- Added composite UniqueConstraint('student_id', 'path_id', name='uq_student_path_note') to StudentNote in app/models/learning.py.
- Prevents duplicate note entries under concurrent write requests in production PostgreSQL environments.
- Verified ORM compatibility across SQLite, MySQL, and PostgreSQL dialects.

References #37
