integration: /curriculum/exercises endpoint for the Challenges

Live Challenge Data:

 - The Challenges (Coding Problems) tab now fetches real exercises from your backend using the newly integrated getAllExercises service.
 - Each challenge card displays the Title, Learning Path, Page Location, and Difficulty Level.

Path-Based Filtering:

 - Dropdown Integration: Replaced the redundant "Add Challenge" button in the header with a "Filter by Path" dropdown.
 - Dynamic Filtering: Just like the Quiz tab, selecting a path instantly filters the challenges to show only those belonging to the chosen curriculum section.

Layout & UX Optimization:

 - Full-Width Design: Challenge cards now utilize the 100% width of the container, creating a uniform and professional look across all tabs.
 - Button Placement: The "Launch Challenge Creator" button is now positioned at the bottom of the list, serving as a clear call-to-action after reviewing existing challenges.
 - Empty States: Added a visually distinct empty state that appears when no challenges match the current filter.


The Curriculum Manager is now a fully data-driven tool, allowing trainers to efficiently browse and filter every component of the JS Mentor syllabus.