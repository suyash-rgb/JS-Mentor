refactor: standardize directory architecture and fix component imports

Description:

Import Resolution: Updated all component and style references to reflect the new nested directory structure (/pages/fundamentals, /pages/bitwise, etc.), ensuring project-wide build stability.

Path Optimization: Corrected relative paths for the shared <Compiler /> and Fundamentals.css across all learning path modules.

Structural Alignment: Finalized the migration of technical lessons into dedicated domain folders to improve codebase scalability and file discovery.

Compiler Integration: Verified the routing and import logic for the safer jscompiler.js implementation to ensure stable handling of document.write() calls.