fix: structural grouping and code-result alignment

- Fixing the issue where explanation text (e.g., title41) was being rendered as a separate card instead of a description within the parent card (title4).
- Overhauling the 'renderDynamicSections' logic to correctly identify child descriptive keys and prevent them from spawning new sections.
- Correcting the code-result mapping: ensured that 'code2' correctly pulls from 'result' and 'code3' from 'result1', etc., fixing the misaligned output labels in the UI.
- Applying these fixes across both Fundamentals and JavaScript Core dynamic components for consistency.