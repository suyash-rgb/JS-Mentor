feat: automated the doubt session scheduling engine

- Integrated APScheduler into FastAPI lifespan events to automate doubt scheduling at 10 AM daily.
- Removed the manual POST /api/v1/schedule/run endpoint to eliminate manual trainer intervention.
- Implemented database-level locking (FOR UPDATE SKIP LOCKED) in the scheduling engine to prevent race conditions in multi-instance deployments.
- Added a standalone script scripts/run_automation.py for manual or external cron triggers.
- Cleaned up unused scheduling schemas and router imports.
- Updated requirements.txt with apscheduler.
