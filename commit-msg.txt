feat: Integrate final examination page and lock logic.

src/Router/AppRouter.js: Registered the new protected route /final-exam for the final examination page.
src/pages/dashboard/student/Dashboard.js: Implemented logic to dynamically check if all 6 learning paths are at 100% progress, and added the conditionally rendered "Final Endgame Examination" banner (showing locked vs. unlocked states).
src/pages/FinalExamPage.js (NEW): Built the complete exam interface handling the 30 questions. Integrated the anti-cheat engine to track sidebar docking/tab switching via resize, blur, and visibilitychange listeners (with a 3-warning limit and failure state).
src/pages/FinalExamPage.css (NEW): Added comprehensive styling for the final exam page, including workspace-blocking glassmorphism overlays for the anti-cheat warnings and locked states.