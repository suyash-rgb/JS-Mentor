fix: curriculum manager naming conflicts

Resolved Method Mismatches:
Fixed a naming discrepancy where the router was calling get_path_structure() but the service layer was using a different name. I've synchronized them so the /curriculum/visualize endpoint is now fully functional.

New Sidebar Endpoint:
Endpoint: GET /curriculum/names
Purpose: This provides a clean list of just the learning path names (e.g., ["Fundamentals", "JS Core", ...]). This is specifically designed to populate the Syllabus Structure sidebar in your Trainer Panel without fetching the massive full curriculum object.

Error Handling:
I've wrapped every single route in app/routers/curriculum.py with robust try...except blocks.
The system now gracefully catches unexpected failures (like data.json being locked or malformed) and returns clear, professional error messages via HTTPException instead of crashing the server.

Schema Alignment:
Ensured that the ex_id and quiz_id parameters across PUT and DELETE routes are handled as strings to match the new UUID-based structure in data.json.