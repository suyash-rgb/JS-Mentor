feat: weekly challenge page

WeeklyChallenge.js:
- Overhauled layout to a 2-column CSS grid, placing the Hero card and Global Leaderboard side-by-side on large screens.
- Enhanced the leaderboard to dynamically highlight the currently logged-in user with a "(You)" badge and distinct background, even fetching their exact rank if outside the top 10.
- Implemented a new "Past Challenges" section below the fold, rendering a grid of historical weekly challenges.

AppRouter.js:
- Registered the new `/practice-hub/all` route, linking it to the PracticeQuestions component as a ProtectedRoute.

