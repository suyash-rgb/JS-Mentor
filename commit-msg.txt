feat: Toolbar controls & Line breaks

Toolbar Controls Added: a neat formatting toolbar right above the editor pane. 
It includes one-click buttons for Bold, Italic, Headings, Bulleted Lists, Numbered Lists, Code blocks, and Links. 
Clicking them will automatically insert the correct markdown syntax at the cursor

Native Line Breaks Supported: installed and configured a markdown plugin called remark-breaks. 
You no longer need to know the "double space at the end of a line" trick. 
If a trainer hits the Enter key to drop down a single line 
it will now render accurately as a single line break on the preview and for the students