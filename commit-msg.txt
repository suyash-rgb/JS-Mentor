refactor: consolidate learning path topics

- Replaced 6 duplicated topic pages with a single dynamic LearningPathTopic.js component.
- Updated AppRouter.js to map all 60 path shortcodes to the unified component.
- Scoped 6 CSS stylesheets (.path-theme-*) to isolate styles and support dynamic theme switching.
- Fixed sidebar navigation to use react-router-dom's useNavigate and useLocation, ensuring correct address updates and accurate progress tracking.
- Resolved jumbled code examples by mapping code blocks deterministically based on card group schemas.
- Restored the JSCompiler CTA for Fundamentals and JS Core paths.
- Added comprehensive unit tests and JSDOM mocks for intersection observers and routing to ensure robust builds.