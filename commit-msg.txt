integrated: /curriculum/quizzes 

Trainer Service Update: Added getAllQuizzes to src/utils/trainerService.js to handle communication with the backend.

Quiz Tab Refactoring:

 - Live Data: The QuizTab now fetches real quiz data from the curriculum. It displays the quiz title, the specific Learning Path and Page it belongs to, and the total number of questions.

 - Dynamic List: Replaced the static placeholders with a mapped list of all quizzes found in the curriculum.

 - Button Re-positioning: Moved the "Create New Quiz" button from the top-right to the bottom of the list, centered for better accessibility once the list grows.

UI Feedback: Added a loading state (CircularProgress) and an empty state message ("No quizzes found") to ensure the interface is intuitive even before data arrives or if the curriculum is empty.

Cleaned Up Warnings:

 - Chatbot.js & Quiz.js: Removed unused variable declarations (fileInputRef and questionText) to keep the console clean.
 Ai.js & Chatbot.js: Fixed an accessibility warning related to how links are rendered within Markdown. Anchor tags now explicitly receive and render their child content, ensuring they are accessible to screen readers.